# Project-level Codex config placeholder.
# Prefer tracked repo guidance in AGENTS.md and reusable skills in .codex/skills.
# Add repo-specific overrides here only when the default global Codex config is not enough.
