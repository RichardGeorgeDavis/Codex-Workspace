# Example Fragment

## Testing

- run the repo's normal test command before handing work off
- call out skipped checks explicitly
- prefer focused verification before broad reruns when the change is small
