# Workspace Quality Gate Skills

This plugin now ships one concrete local skill pack:

- `skills/workspace-quality-gate/SKILL.md`

It is intentionally non-destructive and is meant for release-readiness and
workspace quality checks rather than repo mutation.

Add future plugin-local skills here only when they represent stable,
workspace-specific workflows that are not better expressed as shared skills
under `shared/skills/`.
