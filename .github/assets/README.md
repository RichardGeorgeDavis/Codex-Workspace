# Assets

`social-preview.png` is the repository social preview image sized for GitHub's recommended 1280 x 640 layout.

Use it in:

1. Repository `Settings`
2. `Social preview`
3. `Edit`
4. `Upload an image`

The SVG source is kept in `social-preview.svg` so the preview can be regenerated without hand-editing a binary.
